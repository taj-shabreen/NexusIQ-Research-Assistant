# ── Web framework ─────────────────────────────────────────────────
fastapi>=0.111.0,<0.120.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # required for FastAPI file uploads

# ── LangChain (LLM chain only — no document loaders, no embeddings) ─
langchain-core>=0.2.0,<0.4.0
langchain-groq>=0.1.6

# ── PDF extraction ────────────────────────────────────────────────
pymupdf>=1.24.0                # imported as `fitz` in ingestion.py

# ── Vector store ──────────────────────────────────────────────────
chromadb>=0.5.0,<0.6.0

# ── Embeddings — Jina AI API (free tier, no local model, no torch) ──
# No SDK needed — embeddings.py calls the Jina REST API directly via httpx
# (already listed below under Utilities).

# ── Reranker (CrossEncoder) — DISABLED for this deployment ─────────
# sentence-transformers (and its torch/transformers dependencies) has been
# removed entirely. RERANKER_ENABLED=false in your Render env vars ensures
# app/rag/retriever.py never imports sentence_transformers.CrossEncoder.
# Retrieval still works fully via BM25 + Jina semantic search + RRF fusion;
# you only lose the final cross-encoder reranking pass.
# To re-enable reranking later: add `sentence-transformers>=2.7.0` back here
# and set RERANKER_ENABLED=true (this will reintroduce torch as a dependency).

# ── BM25 ──────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Config / Settings ─────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0